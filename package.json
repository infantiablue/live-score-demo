{
  "name": "live-score-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone API-Football live scoreboard with visibility-aware polling, backoff, and stale-data labels.",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --watch --env-file-if-exists=.env server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22.9"
  }
}
